# Toto je příklad erkového skriptu; obsahuje kód (a kometáře k němu)
# kód seběhne zmáčknutím Source vpravo nahoře, nebo příkazem 
# source("./cvičení/0 - R skript.R") na konzoli
# nebo zkratkou Ctrl + Shift + S
# pro spuštění pouze části kódu použijte Ctrl + Shift + Enter

# do proměnné "text" uložit textovou konstantu (tiše)
text <- "Karel"

# do proměnné "cislo" uložit číselnou konstantu (tiše)
cislo <- c(1, 2, 3)

# na konzoli vypsat "Máma má mísu" (a nikam neukládat)
print("Máma má mísu")

# funkce: na konzoli vypsat obsah existující proměnné text
print(text)

